# Triggers
# Schedule-based triggers for background processing

triggers:
  schedule:
    jobs:
      - cron: "* * * * *"
        workflow: "pipeline:agent-tick"
        action: "tick"
      - cron: "*/5 * * * *"
        workflow: "pipeline:infra-monitor"
        action: "health-check"
